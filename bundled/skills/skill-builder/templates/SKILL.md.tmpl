---
name: {{skill_id}}
display_name: {{中文技能名，UI 显示，4～10 字}}
description: {{一句话：什么场景下启用 + 产出什么。≤60 字，每轮都注入上下文，越短越好}}
version: 1.0.0
tier: light
permissions:
  - workspace_read
  - workspace_write
  - run_python
category: productivity
trigger_phrases:
  - {{用户可能说的原话1}}
  - {{用户可能说的原话2}}
  - {{用户可能说的原话3}}
---

# {{中文技能名}}

{{一句话说清场景与边界：面向什么材料、谁在用、产出什么。}}

## 何时用 / 何时不用

- 用：{{典型触发情形}}
- 不用：{{容易误伤的相邻情形，指明该走哪个技能或直接手办}}

## 输入与产出

```
{{素材位置，如：工作区根/周报素材/*.docx}}
.office-agent/work/{{skill_id}}/     # 过程文件
工作成果/{{最终交付文件名}}             # 交付物
```

## {{N}} 步纪律（必须按序）

### Step 1 — {{步骤名}}（脚本，1 步）

```
run_skill_script
  skill_id: {{skill_id}}
  script: {{脚本名}}.py
  args: [{{参数}}]
```

产出 {{产物}}。向用户报告 {{要汇报的关键数字}}。

### Step 2 — {{步骤名}}（Agent，约 N 步）

1. {{具体动作，指明用哪个工具}}
2. {{具体动作}}

规则：

- {{必须遵守的判断标准}}
- {{禁止项}}

### Step 3 — {{步骤名}}（Agent，1 步）

{{……}}

## 步数预算

| 阶段 | 约计步数 |
|------|----------|
| {{阶段名}} | {{n}} |
| **合计** | **≈{{n}}** |

{{素材超量时的分会话策略：会话 A 做哪几步、会话 B 从哪个落盘产物续跑。}}

## 硬规则

- {{绝对不能违反的业务约束，如口径、字号、脱敏要求}}
- {{常见错误的规避方式}}

## 变更记录

- 1.0.0 — {{首版，固化自哪次任务}}
